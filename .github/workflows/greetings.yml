name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: "👋 Hi there! Thank you for creating an issue. We appreciate your contributions and feedback! Our team will get back to you shortly."
          pr-message: "🎉 Thank you for your first pull request! Your efforts are greatly valued, and we'll review it as soon as possible."
